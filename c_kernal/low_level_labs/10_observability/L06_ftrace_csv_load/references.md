# L06 references

- `Documentation/trace/ftrace.rst` — official ftrace docs.
- `Documentation/trace/events.rst` — event tracepoints (alternative to function_graph).
- LWN, "Secrets of the Linux file systems" (search) — VFS overview.
- `man 2 read` and `man 5 proc` (especially `/proc/sys/vm/drop_caches`).
- "Linux Storage Stack Diagram" (`thomas-krenn.com/en/wiki/Linux_Storage_Stack_Diagram`) — visual overview of what ftrace will reveal.
