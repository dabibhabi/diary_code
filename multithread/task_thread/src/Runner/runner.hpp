/**
 * @file runner.hpp
 * @brief Entry point exposed to the outer main.cpp.
 */
#pragma once

/**
 * @brief Build a 3-worker pool and run the task / worker / pool demo.
 *
 * See runner.cpp for the scenario details.
 */
void runner();
